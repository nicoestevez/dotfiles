[init]
	defaultBranch = main
[core]
	editor = vim
# Machine-specific identity (email, name) lives outside the repo.
# On a new machine, create ~/.gitconfig.local with a [user] section.
[include]
	path = ~/.gitconfig.local
